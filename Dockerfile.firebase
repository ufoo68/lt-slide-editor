FROM node:24-alpine

RUN npm install -g firebase-tools@14

WORKDIR /workspace

COPY firebase.json ./

EXPOSE 4000 9099

CMD ["firebase", "emulators:start", "--only", "auth", "--project", "demo-lt-slide-editor"]
